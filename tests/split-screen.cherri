// compile-only: UI layout, not assertable

menu "Choose App" {
    item "Notes":
        openApp("notes")
    item "Maps":
        openApp("maps")
}

alert("","Split Screen?")

splitApps("notes","maps","thirdByTwo")
